{% extends "layout.html.twig" %}

{% block title %}Home{% endblock %}

{% block content %}
{% for movie in movies %}
<article>
    <h2><a class="articleTitle" href="{{ path('movie', { 'id': movie.id }) }}">{{ movie.title }}</a></h2>
    <p>{{ movie.descriptionCourt }}</p>
</article>
{% endfor %}
{% endblock %}